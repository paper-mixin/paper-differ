[tool.poetry]
name = "paper-differ"
version = "0.1.0"
description = ""
authors = ["xyzeva <xyzeva@riseup.net>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

